// Content Script (src/contentScript.ts): This script is injected into the current webpage. It should not contain React, as that would add unnecessary bloat to every page load.

//     Action: Finds the T&C text (e.g., scraping the DOM), pre-processes it, and sends it to your AI backend for analysis.

//     Tech: Plain TypeScript/JavaScript.

